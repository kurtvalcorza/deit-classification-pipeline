# Release status

Current status: **Candidate — default-path execution recorded on 2026-09-26 (Kaggle T4); REL12 BYOD exercise pending before promotion**. The pipeline package, the offline test suite (including a small random-weight `ViTForImageClassification` run through prediction, the zero-shot baseline, full and frozen fine-tuning, evaluation and adapter reload, and a full-size random checkpoint taken through the strict loader), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2) and the standalone `E2E` tutorial `tutorials/deit_classification_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.1) exist. The upstream snapshot is pinned: `MODEL_REVISION` is `164deee347853469b97442b3817f22eece80c7e3` and the manifest records every file's byte size and SHA-256 (`pytorch_model.bin` `7eadf70de01e…`, matching the Hub's LFS record). The exact commit `4386e6d` / notebook blob `1a0317f0db42` executed top-to-bottom on Kaggle Tesla T4 on 2026-09-26 UTC with both BYOD branches off (348.7 s, 14/14 post-restart code cells): on 60 held-out CIFAR-10 thumbnails the fine-tuned head scored accuracy 1.000 against zero-shot ImageNet mapping 0.983, untrained head 0.483 and majority baseline 0.500, a one-image difference from zero-shot that is within the noise of a 60-image split. The tutorial split is not aware of the sample archive's 100 pixel-identical groups: by the IDs in `deit_classification_predictions.csv`, 46 of 60 held-out and 36 of 60 unseen images have their same-numbered counterpart (`original_images` ↔ `darkened_images`) outside every evaluation split, that is, in train (a count of counterparts, not of confirmed pixel copies). The full record is in `docs/release-verification.md`. Release step 7 (REL12: the BYOD dataset branch with a small class-folder directory and with an incompatible one) has not been exercised, so the status stays Candidate.
